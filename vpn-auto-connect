#!/usr/bin/env sh
# Wrapper que invoca o Python correto e repassa os argumentos.
set -e
SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
exec "${PYTHON:-python3}" "$SCRIPT_DIR/vpn-auto-connect.py" "$@"